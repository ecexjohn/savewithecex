export const CLS_FACTOR = 10000;

export const MARKER = '\u23f1'; // stopwatch

export const LISTEN = {
    capture: true,
    passive: true
};

export const LISTEN_ONCE = Object.assign({
    once: true
}, LISTEN);