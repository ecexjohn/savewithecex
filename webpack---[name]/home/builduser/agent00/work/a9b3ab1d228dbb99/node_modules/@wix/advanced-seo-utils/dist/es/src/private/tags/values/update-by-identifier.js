import {
    TAG_SCHEMAS
} from '../../types/TagSchemas';
import {
    isEmpty
} from '../../utils/is-empty';
import {
    buildTagByValueAndIdentifier
} from '../utils/build-tag-by-value';
import {
    updateValueBySchema
} from './update-by-schema';
export var updateValueByIdentifier = function(tags, identifier, value, meta, enableValidation) {
    if (tags === void 0) {
        tags = [];
    }
    if (enableValidation === void 0) {
        enableValidation = true;
    }
    var tag = buildTagByValueAndIdentifier(value, identifier, {
        enableValidation: enableValidation,
    });
    if (isEmpty(tag)) {
        return tags;
    }
    if (meta) {
        tag.meta = meta;
    }
    var tagSchema = TAG_SCHEMAS[identifier];
    return updateValueBySchema(tags, tagSchema, value, meta);
};